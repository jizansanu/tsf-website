# Public site URL (no trailing slash). REQUIRED for production builds:
# feeds sitemap.xml, robots.txt, canonical URLs, and Open Graph tags.
# Set this in Vercel project settings once the client's domain is live.
NEXT_PUBLIC_SITE_URL=https://tsfone.com
